# 침수퇴로 AI 업그레이드 구현계획

문서 상태: 실행 기준
작성일: 2026-08-31
대상: 2026 AI·디지털 기반 사회문제 해결 챌린지 제출본
전제: 이 문서만 읽고 추가 해석 없이 구현할 수 있도록 작성한다. 판단이 필요한 지점은 모두 "적용 기준"에 명시했다.

---

## 0. 이 문서를 읽는 법

### 0.1 Phase 구성 원칙

Phase는 **의존 순서**로 배열했다. 앞 Phase를 건너뛰면 뒤 Phase가 성립하지 않는다.

| Phase | 이름                | 선행 조건 | 예상 기간 |
| ----- | ------------------- | --------- | --------- |
| 0     | 기반 복구 (완료)    | —         | 완료      |
| 1     | 데이터 신뢰성       | Phase 0   | 3일       |
| 2     | PWA 기반            | Phase 1   | 2일       |
| 3     | 알림 파이프라인     | Phase 2   | 4일       |
| 4     | 긴급 모드·음성 안내 | Phase 1   | 2일       |
| 5     | 목데이터 제거       | Phase 1   | 3일       |
| 6     | 차별화 기능         | Phase 1   | 5일       |
| 7     | 포용성·마무리       | Phase 4   | 3일       |

Phase 2·4·5는 서로 독립이므로 인원이 둘 이상이면 병렬 진행할 수 있다.
Phase 3은 반드시 Phase 2 이후다. 서비스워커가 없으면 웹푸시 구독 자체가 불가능하다.

### 0.2 각 Phase 항목 구성

모든 작업 항목은 아래 6개 절을 갖는다.

- **작업 내용** — 무엇을 만드는가
- **수정 대상** — 건드릴 파일 목록
- **구현 방법** — 어떻게 만드는가
- **적용 기준** — 어떤 값·조건을 쓰는가 (판단이 필요한 지점의 답)
- **예외 처리** — 실패했을 때 어떻게 되는가
- **검증 방법** — 됐는지 어떻게 확인하는가

### 0.3 공통 규칙

이 규칙은 모든 Phase에 적용된다. 각 항목에서 반복하지 않는다.

**실패 처리 원칙**

보조 데이터의 수집 실패가 핵심 기능(위험도 계산, 대피소 추천, 경로 안내)을 막아서는 안 된다.
Edge Function은 상류 실패 시 HTTP 500이 아니라 다음 형태로 응답한다.

```ts
return jsonOk({ /* 빈 결과 */, status: "PENDING_ACCESS", message: "실패 사유" });
```

클라이언트 훅은 `PENDING_ACCESS`를 `ApiStatus`의 `FALLBACK`으로 낮춰 표시하고, 화면은 계속 동작시킨다.
이 패턴의 기준 구현은 [`supabase/functions/weather-warning/index.ts`](supabase/functions/weather-warning/index.ts)와
[`supabase/functions/traffic-events/index.ts`](supabase/functions/traffic-events/index.ts)다.

**핵심 데이터와 보조 데이터 구분**

| 구분 | 대상                                     | 실패 시                                    |
| ---- | ---------------------------------------- | ------------------------------------------ |
| 핵심 | 기상청 단기예보, 대피소, 경로            | 위험도 `UNKNOWN` 처리 또는 fallback 데이터 |
| 보조 | 기상특보, ITS 돌발, CCTV, 센서, 재난문자 | 빈 결과로 진행, 화면에 `대체 데이터` 표시  |

**AI 사용 원칙 (변경 금지)**

위험도 등급, 경로 순위, 대피소 선택은 **서버·클라이언트 계산값**이 정한다.
Gemini는 설명문 생성과 근거 판독에만 쓴다. 이 원칙은 상세서 2.2에 명시돼 있고 심사에서 방어 논리가 된다.
Phase 6의 CCTV 판독도 "판정"이 아니라 "근거 가산"으로 설계한다.

**코딩 규약**

- 줄바꿈은 LF. Windows에서 Python 스크립트로 파일을 쓰면 CRLF가 섞이므로, 수정 후 반드시 아래를 실행한다.
  ```bash
  npx prettier --write <수정한 파일들>
  ```
- 새 모듈에는 같은 디렉터리에 `*.test.ts`를 만든다.
- Edge Function 공용 로직은 `supabase/functions/_shared/`에 두고, 순수 함수로 분리해 vitest로 테스트한다.
  (Deno 런타임 의존 코드는 `index.ts`에만 둔다. `_shared`의 순수 모듈은 vitest가 그대로 import한다.)
- 새 Edge Function은 반드시 두 곳에 등록한다.
  - `supabase/config.toml` — `[functions.<이름>]` 블록
  - `scripts/deploy-supabase-all.ps1` — 함수 이름 배열

**테스트 실행**

```bash
npx vitest run                          # 전체
npx vitest run src/lib/risk/            # 디렉터리
npx tsc --noEmit -p tsconfig.json       # 타입체크
npx eslint <경로>                        # 린트
```

기준선: **50개 파일 212개 테스트 통과** (2026-08-31 시점). 이 수치가 줄면 회귀다.

`--reporter=basic`은 vitest 4에서 제거됐다. 쓰지 말 것.

---

## Phase 0. 기반 복구 (완료)

기록용. 후속 Phase가 이 결과를 전제로 한다.

### 0-A. 테스트 스위트 복구 (완료)

`vitest run`이 `@vitest/utils`를 찾지 못해 실행 자체가 불가능했다.
원인은 bun이 생성한 `node_modules`를 pnpm이 제거하려다 TTY 부재로 설치가 중단된 것이었다
(`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`).

```bash
CI=true pnpm install
```

결과: 50개 파일 212개 테스트 통과.

**남은 일**: `bun.lock`이 아직 git에 추적 중이다. Phase 1-D에서 정리한다.

### 0-B. 기상청 기상특보 연동 (완료)

- 신규: `supabase/functions/_shared/kmaWarning.ts`, `supabase/functions/weather-warning/index.ts`,
  `src/lib/api/weatherWarning.ts`, `src/hooks/useWeatherWarnings.ts`,
  `scripts/sync-warning-zones.js`, `public/data/warning-zones.json`(301건)
- 제거: `_shared/kma.ts`가 강수량으로 합성하던 "강수 관측" 가짜 경보
- 연결: `calculateRiskScore`의 기상 항목, `WeatherPanel` 표시

상세는 [`docs/api-integrations.md`](docs/api-integrations.md)의 "기상청 기상특보" 절 참조.

**남은 일**: 공공데이터포털 활용신청 승인. 승인 전에는 `PENDING_ACCESS`로 동작한다.

---

## Phase 1. 데이터 신뢰성

> 심사위원이 화면에서 가장 먼저 확인하는 값들을 진짜로 만든다.
> 이 Phase를 건너뛰면 뒤에서 무엇을 하든 "이 앱의 숫자는 못 믿겠다"가 된다.

### 1-A. 데이터 기준시각 실제화

#### 작업 내용

화면에 표시되는 "데이터 기준시각"을 고정 상수에서 각 API의 실제 응답 시각으로 교체한다.

현재 `src/mocks/data.ts`의 `DATA_TIMESTAMP = "2026-06-11T14:30:00+09:00"`이 홈·경로·운영·안내문
화면에서 기준시각으로 표시된다. 상세서 4.1의 완료 조건이 "데이터 기준시각 표시"인데 값이 과거에 고정돼 있다.

#### 수정 대상

| 파일                                 | 라인           | 현재                              | 변경 후                    |
| ------------------------------------ | -------------- | --------------------------------- | -------------------------- |
| `src/lib/api/dataTimestamp.ts`       | 신규           | —                                 | 시각 집계 유틸             |
| `src/routes/index.tsx`               | 77             | `DATA_TIMESTAMP` fallback         | 경로 결과 시각             |
| `src/routes/index.tsx`               | 346            | `ActionCard timestamp`            | 집계 시각                  |
| `src/routes/routes.tsx`              | 191            | `formatTimestamp(DATA_TIMESTAMP)` | 경로 응답 시각             |
| `src/routes/help.tsx`                | 57             | `dataTimestamp`                   | 집계 시각 (Phase 5와 함께) |
| `src/routes/ops/data-health.tsx`     | 34·40·47·63·70 | `lastSuccess`                     | 실제 시각 (Phase 5-C)      |
| `src/routes/ops/messages.tsx`        | 35·58          | `dataTimestamp`                   | 집계 시각 (Phase 5-B)      |
| `src/components/risk/ActionCard.tsx` | —              | `timestamp: string`               | 시각 + 신선도 표시         |

`src/routes/ops/shelters.tsx`의 28·50번 줄은 기준시각이 아니라 **가짜 운영 데이터 생성용**이다.
Phase 5-B에서 통째로 제거하므로 여기서는 건드리지 않는다.

#### 구현 방법

**1) 집계 유틸을 만든다.**

```ts
// src/lib/api/dataTimestamp.ts
import type { ApiResult, ApiStatus } from "@/lib/api/types";

export interface SourceTimestamp {
  label: string; // "기상청 단기예보"
  timestamp: string | null;
  status: ApiStatus;
}

/** 여러 소스 중 가장 오래된 성공 시각. 화면 대표 기준시각으로 쓴다. */
export const oldestSuccessfulTimestamp = (sources: SourceTimestamp[]): string | null => {
  const times = sources
    .filter((s) => s.status === "OK" && s.timestamp)
    .map((s) => new Date(s.timestamp!).getTime())
    .filter(Number.isFinite);
  return times.length > 0 ? new Date(Math.min(...times)).toISOString() : null;
};

/** "3분 전" 형태의 상대 표기. */
export const relativeAge = (timestamp: string | null, now = Date.now()): string => {
  if (!timestamp) return "확실한 정보 없음";
  const diffMs = now - new Date(timestamp).getTime();
  if (!Number.isFinite(diffMs) || diffMs < 0) return "확실한 정보 없음";
  const minutes = Math.floor(diffMs / 60_000);
  if (minutes < 1) return "방금";
  if (minutes < 60) return `${minutes}분 전`;
  const hours = Math.floor(minutes / 60);
  if (hours < 24) return `${hours}시간 전`;
  return `${Math.floor(hours / 24)}일 전`;
};

/** 신선도 등급. 화면에서 색으로 구분한다. */
export const freshness = (timestamp: string | null, now = Date.now()) => {
  if (!timestamp) return "UNKNOWN" as const;
  const minutes = (now - new Date(timestamp).getTime()) / 60_000;
  if (minutes < 0) return "UNKNOWN" as const;
  if (minutes <= 10) return "FRESH" as const;
  if (minutes <= 60) return "AGING" as const;
  return "STALE" as const;
};
```

**2) 각 훅이 이미 시각을 반환한다.** 새로 만들 필요 없다.

| 훅                    | 시각 필드                                           |
| --------------------- | --------------------------------------------------- |
| `useWeather`          | `result.timestamp`                                  |
| `useDisasterMessages` | `result.timestamp`                                  |
| `useWeatherWarnings`  | `result.timestamp` (특보 발표시각)                  |
| `useTrafficEvents`    | `result.timestamp`                                  |
| `useRoutes`           | `results.walk.timestamp`, `results.drive.timestamp` |
| `useSensorFeeds`      | 피드별 `observedAt`                                 |

**3) `useRiskAssessment`가 소스 목록을 함께 반환하게 한다.**

```ts
// src/hooks/useRiskAssessment.ts 반환값에 추가
dataSources: [
  { label: "기상청 단기예보", timestamp: weatherResult.timestamp, status: weatherResult.status },
  { label: "기상청 기상특보", timestamp: warningResult.timestamp, status: warningResult.status },
  { label: "긴급재난문자", timestamp: disasterResult.timestamp, status: disasterResult.status },
] satisfies SourceTimestamp[],
```

**4) `ActionCard`에 신선도 표시를 넣는다.**

기존 `timestamp: string` prop은 유지하고, `timestamp: string | null`로 넓힌 뒤
`relativeAge`로 "N분 전"을 절대시각 옆에 함께 보여준다.

#### 적용 기준

- **대표 기준시각**: 여러 소스 중 **가장 오래된 성공 시각**을 쓴다. 가장 최신을 쓰면 실제보다
  데이터가 신선해 보여 사용자를 오도한다. 재난 서비스는 보수적으로 표기한다.
- **신선도 구간**: 10분 이내 `FRESH`, 60분 이내 `AGING`, 초과 `STALE`.
  기상청 초단기실황이 10분 주기라 이 값을 기준으로 잡는다.
- **표기 형식**: `2026-08-31 17:00 (3분 전)`. 절대시각과 상대시각을 함께 쓴다.
  상대시각만 쓰면 화면 캡처에서 시점을 알 수 없고, 절대시각만 쓰면 신선도가 직관적이지 않다.
- **`STALE` 표시**: 회색 + "데이터 지연" 배지. 숨기지 않는다.

#### 예외 처리

| 상황                  | 처리                                                          |
| --------------------- | ------------------------------------------------------------- |
| 모든 소스 실패        | `"확실한 정보 없음"` 표시. 임의 시각을 만들지 않는다          |
| 시각 파싱 실패        | `null` 취급 → `"확실한 정보 없음"`                            |
| 미래 시각 (시계 오차) | `UNKNOWN` 처리. 음수 경과시간을 "방금"으로 쓰지 않는다        |
| 일부만 성공           | 성공한 소스만으로 집계하고, 실패 소스는 상태 배지로 별도 표시 |

#### 검증 방법

1. **단위 테스트** — `src/lib/api/dataTimestamp.test.ts` 신규 작성
   - `oldestSuccessfulTimestamp`가 FAILED 소스를 제외하는지
   - `relativeAge` 경계값: 0분·59분·60분·23시간·24시간
   - 미래 시각 입력 시 `UNKNOWN`
   - 빈 배열 입력 시 `null`
2. **회귀 검사** — `grep -rn "DATA_TIMESTAMP" src --include="*.tsx" | grep -v test`
   결과가 `ops/shelters.tsx` 2건(Phase 5에서 제거)만 남아야 한다.
3. **육안 확인** — 앱 실행 후 홈 화면 기준시각이 현재 시각 기준 10분 이내로 표시되는지.
   네트워크를 끊고 새로고침하면 `"확실한 정보 없음"`이 나와야 한다.

---

### 1-B. 교통통제 위험 인자 배선

#### 작업 내용

`useRiskAssessment`에서 `trafficControl: false`로 하드코딩된 값을 실제 ITS 돌발상황 데이터로 채운다.

데이터는 이미 받아오고 있다. `useTrafficEvents`가 지도 마커와 경로 감점에는 쓰이는데,
홈 화면 위험도 계산에는 연결되지 않았다. **배선만 하면 되는 작업이다.**

#### 수정 대상

- `src/hooks/useRiskAssessment.ts` — `useTrafficEvents` 호출 추가, `trafficControl` 산출
- `src/lib/risk/trafficEventRisk.ts` — 반경 판정 헬퍼 추가
- `src/lib/risk/trafficEventRisk.test.ts` — 신규

#### 구현 방법

**1) 반경 내 통제 이벤트 판정 헬퍼를 추가한다.**

```ts
// src/lib/risk/trafficEventRisk.ts 에 추가
import { haversineMeters } from "@/lib/utils";
import type { LatLng, TrafficEvent } from "@/lib/types";

/** 현재 위치 반경 내에 통행 차단급 돌발상황이 있는지 판정한다. */
export const hasBlockingEventNearby = (
  events: TrafficEvent[],
  origin: LatLng,
  radiusMeters = TRAFFIC_CONTROL_RADIUS_M,
) =>
  events.some(
    (event) =>
      classifyTrafficEvent(event) === "BLOCKING" &&
      haversineMeters(origin, event.position) <= radiusMeters,
  );

export const TRAFFIC_CONTROL_RADIUS_M = 1_000;
```

**2) `useRiskAssessment`에서 호출한다.**

```ts
const { events: trafficEvents, result: trafficResult } = useTrafficEvents(origin);

// ...
trafficControl: hasBlockingEventNearby(trafficEvents, origin),
```

**3) 근거 문구를 구체화한다.**

`calculateRiskScore`의 `trafficControl > 0 ? "교통통제·돌발"` 문구를 실제 도로명이 있으면
그것으로 대체한다. `trafficEventReason(event)`가 이미 `"강남대로 침수 통제중"` 형태를 만든다.
`RiskCalculationInput`에 `trafficControlTitle?: string`을 추가해 전달한다.

#### 적용 기준

- **반경**: 1,000m. 경로 랭킹의 `TRAFFIC_EVENT_MATCH_RADIUS_M`(120m)보다 넓게 잡는다.
  경로 판정은 "이 길이 막혔나"라 좁아야 하지만, 홈 위험도는 "이 동네 상황이 어떤가"라 넓어야 한다.
- **대상 등급**: `BLOCKING`만. `CAUTION`(사고·공사·정체)은 침수 위험도와 무관하므로 제외한다.
  `classifyTrafficEvent`의 기존 키워드 분류를 그대로 쓴다.
- **점수**: 기존 `calculateRiskScore`의 5점을 유지한다. 배점 재조정은 이 작업 범위가 아니다.

#### 예외 처리

| 상황                          | 처리                                                                                     |
| ----------------------------- | ---------------------------------------------------------------------------------------- |
| ITS 미승인 (`PENDING_ACCESS`) | `trafficControl: false`. 위험도를 낮추는 방향이므로 안전                                 |
| `events` 빈 배열              | `false`                                                                                  |
| 좌표 없는 이벤트              | `haversineMeters`가 `NaN` 반환 → 비교 false → 제외됨. 별도 처리 불필요하나 테스트로 고정 |

> **주의**: ITS 실패를 "위험 없음"으로 처리하는 것은 위험을 과소평가할 수 있다.
> 그러나 이 인자는 전체 100점 중 5점이고, ITS 실패 시 `useApiStatus`가 `FALLBACK`을 표시하므로
> 사용자가 데이터 결손을 인지할 수 있다. 현재 구조에서는 이 처리가 맞다.

#### 검증 방법

1. **단위 테스트** — `hasBlockingEventNearby`
   - 반경 내 `BLOCKING` → `true`
   - 반경 내 `CAUTION`만 → `false`
   - 반경 밖 `BLOCKING` → `false`
   - 빈 배열 → `false`
   - 좌표가 `NaN`인 이벤트 → `false`
2. **통합 테스트** — `calculateRiskScore`에 `trafficControl: true` 전달 시 총점 +5, 근거 문구 포함
3. **회귀** — `grep -n "trafficControl: false" src/hooks/useRiskAssessment.ts` 결과 없음

---

### 1-C. CI 파이프라인 구축

#### 작업 내용

`.github` 디렉터리가 없다. PR마다 lint·typecheck·test를 자동 실행한다.

#### 수정 대상

- `.github/workflows/ci.yml` — 신규

#### 구현 방법

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - run: npx tsc --noEmit -p tsconfig.json
      - run: pnpm test
```

#### 적용 기준

- **typecheck는 경고로 시작한다.** 현재 `src/hooks/useTrafficEvents.ts`와 `src/routes/-index.test.tsx`에
  기존 타입 오류가 있다. 이걸 먼저 고치지 않으면 CI가 첫날부터 빨간불이라 아무도 안 본다.
  두 가지 선택지 중 하나를 고른다.
  - (권장) Phase 1 안에서 기존 오류를 먼저 고치고 typecheck를 필수로 건다.
  - 임시로 `continue-on-error: true`를 붙이고 Phase 7에서 해제한다.
- **`--frozen-lockfile`** 사용. 락파일과 `package.json` 불일치를 CI에서 잡는다.

#### 예외 처리

`pnpm install`이 CI에서 `node_modules` 제거 프롬프트로 멈추는 문제는 GitHub Actions가
`CI=true`를 기본 설정하므로 발생하지 않는다.

#### 검증 방법

1. 의도적으로 실패하는 테스트를 넣고 PR을 열어 CI가 빨간불인지 확인
2. 되돌린 뒤 초록불 확인
3. `pnpm install --frozen-lockfile`이 로컬에서 통과하는지 확인 (락파일 정합성)

---

### 1-D. 저장소 정리

#### 작업 내용

심사에서 소스를 열어볼 경우를 대비한 정리.

#### 수정 대상

| 대상                                   | 현재                  | 조치                    |
| -------------------------------------- | --------------------- | ----------------------- |
| `bun.lock`                             | git 추적 중           | 삭제. pnpm으로 통일     |
| `test_*.js`, `test-api.js` (루트 17개) | git 추적 중           | `scripts/probe/`로 이동 |
| `test.json`, `test2.json`              | 빈 파일, 추적 중      | 삭제                    |
| `tmp-vite-*.log`                       | 최대 3MB, gitignore됨 | 로컬 삭제               |
| `.playwright-mcp/`                     | 로그 다수             | gitignore 추가          |

#### 구현 방법

```bash
git rm --cached bun.lock && rm bun.lock
mkdir -p scripts/probe && git mv test_*.js test-api.js scripts/probe/
git rm test.json test2.json
rm -f tmp-vite-*.log
echo ".playwright-mcp/" >> .gitignore
```

`scripts/probe/README.md`를 만들어 이 스크립트들이 자동화 테스트가 아니라
**API 수동 확인용 도구**임을 명시한다. 이름이 `test_`로 시작해 vitest 테스트와 혼동된다.

#### 적용 기준

- `bun.lock` 삭제 전 `pnpm install --frozen-lockfile`이 성공하는지 먼저 확인한다.
- `package.json`에 `"packageManager": "pnpm@10.33.0"` 필드를 추가해 매니저를 고정한다.
  Phase 0의 장애가 재발하지 않게 하는 근본 조치다.

#### 예외 처리

`scripts/probe/`의 스크립트가 `.env`를 읽는다면 경로가 한 단계 깊어지므로
`dotenv` 경로를 `../../.env`로 수정해야 한다. 이동 후 하나씩 실행해 확인한다.

#### 검증 방법

1. `git ls-files | grep -E "^(test|bun\.lock)"` 결과 없음
2. `rm -rf node_modules && pnpm install --frozen-lockfile && pnpm test` 전체 통과
3. `git status --short`에 의도치 않은 삭제가 없는지 확인

---

## Phase 2. PWA 기반

> 알림과 오프라인의 공통 전제. 이것 없이는 Phase 3을 시작할 수 없다.
> 상세서가 "모바일 PWA 우선"을 표방하는데 매니페스트도 서비스워커도 없는 상태다.

### 2-A. 서비스워커·매니페스트 도입

#### 작업 내용

`vite-plugin-pwa`로 서비스워커와 웹 앱 매니페스트를 생성하고, 핵심 자산을 프리캐시한다.

#### 수정 대상

- `package.json` — `vite-plugin-pwa` devDependency 추가
- `vite.config.ts` — 플러그인 등록
- `public/icons/` — 아이콘 192·512 (maskable 포함)
- `src/routes/__root.tsx` — 매니페스트 링크, theme-color 메타

#### 구현 방법

```ts
// vite.config.ts
import { VitePWA } from "vite-plugin-pwa";

VitePWA({
  registerType: "prompt",
  manifest: {
    name: "침수퇴로 AI",
    short_name: "침수퇴로",
    description: "침수 위험 상황에서 가장 안전한 대피소와 퇴로를 안내합니다",
    lang: "ko",
    start_url: "/",
    display: "standalone",
    background_color: "#ffffff",
    theme_color: "#0b6fd1",
    icons: [
      { src: "/icons/icon-192.png", sizes: "192x192", type: "image/png" },
      { src: "/icons/icon-512.png", sizes: "512x512", type: "image/png" },
      { src: "/icons/maskable-512.png", sizes: "512x512", type: "image/png", purpose: "maskable" },
    ],
  },
  workbox: {
    globPatterns: ["**/*.{js,css,html,woff2}"],
    navigateFallbackDenylist: [/^\/api/, /^\/functions/],
    runtimeCaching: [
      {
        // 대피소·특보구역 정적 데이터: 오프라인에서도 열려야 한다
        urlPattern: /\/data\/.*\.json$/,
        handler: "StaleWhileRevalidate",
        options: { cacheName: "static-data", expiration: { maxAgeSeconds: 604800 } },
      },
      {
        // 지도 타일: 용량이 크므로 보수적으로
        urlPattern: /^https:\/\/.*\.map\.naver\.(com|net)\/.*/,
        handler: "CacheFirst",
        options: { cacheName: "map-tiles", expiration: { maxEntries: 300, maxAgeSeconds: 86400 } },
      },
    ],
  },
});
```

#### 적용 기준

- **`registerType: "prompt"`** 를 쓴다. `autoUpdate`는 사용 중에 페이지가 갑자기 갱신될 수 있는데,
  대피 안내 중에 화면이 바뀌면 위험하다. 갱신은 사용자가 명시적으로 수락하게 한다.
- **Edge Function 응답은 프리캐시하지 않는다.** 재난 데이터를 오래된 캐시로 보여주면 안 된다.
  실시간 데이터의 오프라인 처리는 2-B에서 "마지막 확인 시각"을 명시하는 방식으로 따로 다룬다.
- **`/data/*.json`은 `StaleWhileRevalidate`.** 대피소 16,154건과 특보구역 301건은 자주 안 바뀌고,
  오프라인에서 반드시 열려야 하는 데이터다.
- **아이콘**: 512×512 원본에서 생성. `maskable`은 안전영역 80%를 지켜 여백을 둔다.

#### 예외 처리

| 상황                       | 처리                                                                         |
| -------------------------- | ---------------------------------------------------------------------------- |
| 서비스워커 미지원 브라우저 | 등록 실패를 무시하고 일반 웹앱으로 동작. `"serviceWorker" in navigator` 가드 |
| 개발 모드                  | `devOptions.enabled: false`. HMR과 충돌한다                                  |
| 캐시 용량 초과             | `expiration.maxEntries`로 상한. 초과 시 workbox가 LRU 제거                   |
| 갱신 감지                  | 배너로 "새 버전이 있습니다 · 새로고침" 노출. 강제 리로드 금지                |

#### 검증 방법

1. `pnpm build && pnpm preview` 후 DevTools > Application > Manifest에 오류 없음
2. Service Workers 패널에서 등록 상태 `activated`
3. Lighthouse PWA 감사 통과 (installable 항목)
4. 네트워크 오프라인 전환 후 새로고침 → 앱 셸이 뜨는지
5. `public/data/shelters.json`이 오프라인에서 응답되는지 (Network 탭에서 `from ServiceWorker`)

---

### 2-B. 오프라인 대응

#### 작업 내용

네트워크 단절 시 마지막으로 확인한 정보를 **오래됐다는 사실과 함께** 보여준다.

침수 상황에서는 기지국과 전력이 먼저 나간다. 지금은 `navigator.onLine`을 쓰는 곳이 없고
zustand에 persist가 없어 새로고침 한 번에 전부 초기화된다.

#### 수정 대상

- `src/hooks/useOnlineStatus.ts` — 신규
- `src/components/layout/OfflineBanner.tsx` — 신규
- `src/store/scenario.ts` — persist 미들웨어 적용
- `src/router.tsx` 또는 QueryClient 생성부 — `persistQueryClient` 적용
- `package.json` — `@tanstack/query-persist-client`, `@tanstack/query-sync-storage-persister`

#### 구현 방법

**1) 온라인 상태 훅**

```ts
// src/hooks/useOnlineStatus.ts
import { useEffect, useState } from "react";

export const useOnlineStatus = () => {
  const [online, setOnline] = useState(() =>
    typeof navigator === "undefined" ? true : navigator.onLine,
  );

  useEffect(() => {
    const on = () => setOnline(true);
    const off = () => setOnline(false);
    window.addEventListener("online", on);
    window.addEventListener("offline", off);
    return () => {
      window.removeEventListener("online", on);
      window.removeEventListener("offline", off);
    };
  }, []);

  return online;
};
```

**2) zustand persist** — 위치·위험도·마지막 추천만 저장한다.

```ts
// src/store/scenario.ts
persist(storeCreator, {
  name: "chimsu-scenario",
  partialize: (state) => ({
    origin: state.origin,
    locationStatus: state.locationStatus,
    riskLevel: state.riskLevel,
    riskScore: state.riskScore,
  }),
});
```

**3) React Query 캐시 영속화** — `maxAge`를 24시간으로 두되, 화면에는 항상 나이를 표시한다.

#### 적용 기준

- **저장 대상**: 위치, 위험도 등급·점수, 마지막 추천 대피소·경로, 행동요령 텍스트.
- **저장 제외**: 사용자 식별 정보, 정확 좌표의 장기 보관.
  상세서 2.2가 "사용자 위치 상시 추적"을 제외 범위로 명시했다. `partialize`로 최소 필드만 저장한다.
- **오프라인 표시 문구**: `"오프라인 · 마지막 확인 17:02 (23분 전)"`.
  단순히 "오프라인"만 쓰면 화면의 위험도가 지금 값인지 옛날 값인지 알 수 없다.
- **1시간 초과 데이터**: 위험도 배지를 회색으로 낮추고 `"정보가 오래됐습니다"` 문구를 얹는다.
  옛날 "안전"을 지금 "안전"처럼 보여주면 안 된다.

#### 예외 처리

| 상황                                     | 처리                                                             |
| ---------------------------------------- | ---------------------------------------------------------------- |
| `localStorage` 접근 불가 (프라이빗 모드) | try/catch로 감싸고 메모리 전용으로 동작                          |
| 저장 데이터 스키마 불일치 (앱 갱신 후)   | zustand `version` + `migrate`로 처리. 실패 시 초기화             |
| `navigator.onLine`이 true인데 실제 단절  | 쿼리 실패가 곧 신호. 연속 2회 실패 시 오프라인으로 간주          |
| 캐시 만료 (24시간 초과)                  | 캐시를 버리고 빈 상태로 시작. 오래된 재난 정보를 보여주지 않는다 |

#### 검증 방법

1. DevTools 오프라인 전환 → 배너 노출, 마지막 확인 시각 표시
2. 오프라인 상태로 새로고침 → 대피소 목록과 마지막 위험도가 유지되는지
3. 1시간 이상 지난 캐시를 시뮬레이션(시스템 시계 조정 또는 저장값 조작)해 회색 처리 확인
4. 프라이빗 모드에서 앱이 크래시하지 않는지
5. 단위 테스트: `useOnlineStatus` 이벤트 리스너 등록/해제

---

## Phase 3. 알림 파이프라인

> 재난 서비스에서 단일 최고가치 기능. 사용자가 앱을 열지 않아도 위험을 알리는 유일한 경로다.
> 현재 4중으로 끊겨 있다.

### 3-A. 현재 상태 진단

착수 전 반드시 확인할 것. 네 곳이 모두 끊겨 있어 하나만 고쳐서는 동작하지 않는다.

| #   | 문제                                                            | 위치                                      |
| --- | --------------------------------------------------------------- | ----------------------------------------- |
| 1   | 서비스워커 없음 → `navigator.serviceWorker.ready`가 영원히 대기 | Phase 2에서 해결                          |
| 2   | `initPushNotifications`를 호출하는 코드가 없음                  | `src/lib/push/pushService.ts`             |
| 3   | 레거시 FCM 엔드포인트 사용 (2024년 종료)                        | `supabase/functions/push-notify/index.ts` |
| 4   | `device_tokens` 테이블이 존재하지 않음 (`as any`로 가려져 있음) | 마이그레이션 없음                         |
| 5   | 위험도 변화를 감지해 발송하는 스케줄러 없음                     | —                                         |

### 3-B. 구독 저장소 구축

#### 작업 내용

웹푸시 구독 정보를 저장할 테이블을 만든다.

#### 수정 대상

- `supabase/migrations/<타임스탬프>_create_push_subscriptions.sql` — 신규
- `src/integrations/supabase/types.ts` — 타입 재생성

#### 구현 방법

```sql
create table public.push_subscriptions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade,
  endpoint text not null unique,
  p256dh text not null,
  auth text not null,
  -- 위험도 재계산 기준 좌표. 행정동 수준으로 낮춰 저장한다.
  region_lat double precision,
  region_lng double precision,
  alert_threshold text not null default 'WARNING'
    check (alert_threshold in ('WATCH', 'WARNING', 'CRITICAL')),
  -- 같은 등급 반복 발송 억제용
  last_notified_level text,
  last_notified_at timestamptz,
  failure_count integer not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

alter table public.push_subscriptions enable row level security;

create policy "push_subscriptions_manage_own"
on public.push_subscriptions for all to authenticated
using (auth.uid() = user_id) with check (auth.uid() = user_id);

create index push_subscriptions_region_idx
  on public.push_subscriptions (region_lat, region_lng)
  where region_lat is not null;
```

기존 `notification_preferences` 테이블의 `alert_threshold`와 중복이 있다.
`notification_preferences`는 사용자 단위 설정, `push_subscriptions`는 기기 단위 구독으로 역할을 나눈다.
발송 시 임계값은 `push_subscriptions` 쪽을 쓰고, 사용자가 설정을 바꾸면 양쪽을 함께 갱신한다.

#### 적용 기준

- **좌표 정밀도**: 소수점 3자리(약 110m)로 낮춰 저장한다. 정확 좌표 상시 보관은
  상세서 2.2의 제외 범위에 저촉된다. 위험도 판정에는 110m 해상도면 충분하다.
- **`endpoint` unique**: 같은 기기의 중복 구독을 막는다. 재구독 시 upsert.
- **익명 사용자**: `user_id`를 nullable로 둔다. 로그인 없이도 알림을 받을 수 있어야
  시민용 서비스로 성립한다. RLS는 익명 구독에 대해 Edge Function(service role)만 접근하게 한다.

#### 예외 처리

| 상황                 | 처리                                       |
| -------------------- | ------------------------------------------ |
| 동일 endpoint 재구독 | `on conflict (endpoint) do update`         |
| 구독 만료 (410 Gone) | 발송 시 감지해 행 삭제                     |
| 발송 연속 실패       | `failure_count` 증가, 5회 초과 시 비활성화 |

#### 검증 방법

1. `pnpm supabase db push` 후 테이블 생성 확인
2. RLS 정책 테스트: 다른 사용자의 구독을 조회·수정할 수 없는지
3. 같은 endpoint로 두 번 insert 시 중복 행이 생기지 않는지

---

### 3-C. VAPID 웹푸시 전환

#### 작업 내용

레거시 FCM 호출을 표준 웹푸시(VAPID)로 교체하고, 구독 등록 흐름을 실제로 연결한다.

#### 수정 대상

- `supabase/functions/push-notify/index.ts` — 전면 재작성
- `supabase/functions/push-subscribe/index.ts` — 신규
- `src/lib/push/pushService.ts` — `device_tokens` → `push_subscriptions`, 실제 호출 연결
- `src/components/notifications/NotificationConsentCard.tsx` — `initPushNotifications` 호출
- `.env.example` — `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`

#### 구현 방법

**1) VAPID 키 생성**

```bash
npx web-push generate-vapid-keys
```

공개키는 `VITE_VAPID_PUBLIC_KEY`(클라이언트), 개인키는 `VAPID_PRIVATE_KEY`(Supabase secret)로 등록한다.

**2) Deno용 웹푸시** — `push-notify`에서 `npm:web-push` 또는 Deno 호환 라이브러리를 쓴다.
`deno.json`의 `imports`에 등록한다.

**3) 클라이언트 구독 등록** — 기존 `pushService.ts`의 흐름은 맞다. 저장 대상만 바꾸고
`as any` 캐스트를 제거한 뒤, `NotificationConsentCard`에서 실제로 호출한다.

**중요**: 현재 `initPushNotifications`는 **어디서도 호출되지 않는다.** 동의 카드의
버튼 핸들러에 연결하는 것이 이 작업의 핵심이다.

#### 적용 기준

- **권한 요청 시점**: 앱 최초 진입 시 즉시 요청하지 않는다. 사용자가 동의 카드에서
  명시적으로 켤 때만 요청한다. 즉시 요청은 거부율이 높고, 한번 거부되면 되돌리기 어렵다.
- **`userVisibleOnly: true`** 필수. 브라우저 정책상 무음 푸시는 불가하다.
- **발송 주체**: Edge Function만. 클라이언트가 직접 발송하지 않는다.

#### 예외 처리

| 상황                                  | 처리                                                                 |
| ------------------------------------- | -------------------------------------------------------------------- |
| 권한 거부                             | 동의 카드에 "브라우저 설정에서 변경 가능" 안내. 재요청 반복 금지     |
| iOS Safari (홈화면 추가 전 푸시 불가) | 기능 감지 후 "홈 화면에 추가하면 알림을 받을 수 있습니다" 안내       |
| 구독 만료(410)                        | 해당 행 삭제 후 다음 진입 시 재구독                                  |
| VAPID 키 미설정                       | `push-notify`가 `PENDING_ACCESS` 반환. 발송만 비활성, 앱은 정상 동작 |

#### 검증 방법

1. 동의 카드에서 알림 켜기 → `push_subscriptions`에 행 생성 확인
2. Supabase 대시보드에서 `push-notify`를 수동 호출해 실제 알림 수신 확인
3. 구독 해제 → 행 삭제 확인
4. 권한 거부 상태에서 앱이 크래시하지 않는지
5. 단위 테스트: `urlBase64ToUint8Array` 변환 정확성

---

### 3-D. 위험도 변화 감시·발송

#### 작업 내용

주기적으로 구독자 위치의 위험도를 재계산하고, **등급이 올라간 순간에만** 알림을 보낸다.

#### 수정 대상

- `supabase/functions/risk-monitor/index.ts` — 신규
- `supabase/migrations/<타임스탬프>_enable_pg_cron.sql` — 신규
- `supabase/functions/_shared/riskNotification.ts` — 발송 판정 로직 (순수 함수, 테스트 대상)

#### 구현 방법

**1) 발송 판정을 순수 함수로 분리한다.** 이 로직이 알림 품질을 좌우하므로 반드시 테스트한다.

```ts
// supabase/functions/_shared/riskNotification.ts
export type RiskLevel = "SAFE" | "WATCH" | "WARNING" | "CRITICAL" | "UNKNOWN";

const RANK: Record<RiskLevel, number> = {
  UNKNOWN: -1,
  SAFE: 0,
  WATCH: 1,
  WARNING: 2,
  CRITICAL: 3,
};

export interface NotifyDecisionInput {
  currentLevel: RiskLevel;
  lastNotifiedLevel: RiskLevel | null;
  lastNotifiedAt: string | null;
  threshold: RiskLevel;
  now: number;
}

export const RENOTIFY_COOLDOWN_MS = 60 * 60 * 1000;

export const shouldNotify = (input: NotifyDecisionInput): boolean => {
  const { currentLevel, lastNotifiedLevel, lastNotifiedAt, threshold, now } = input;

  // 알 수 없는 상태로는 발송하지 않는다.
  if (currentLevel === "UNKNOWN") return false;
  // 사용자가 설정한 임계값 미만이면 발송하지 않는다.
  if (RANK[currentLevel] < RANK[threshold]) return false;
  // 처음 도달했으면 발송한다.
  if (!lastNotifiedLevel) return true;
  // 등급이 올라갔으면 발송한다.
  if (RANK[currentLevel] > RANK[lastNotifiedLevel]) return true;
  // 같은 등급이면 쿨다운이 지난 경우에만 재발송한다.
  if (RANK[currentLevel] === RANK[lastNotifiedLevel]) {
    if (!lastNotifiedAt) return true;
    return now - new Date(lastNotifiedAt).getTime() >= RENOTIFY_COOLDOWN_MS;
  }
  // 등급이 내려갔으면 발송하지 않는다. 기록만 갱신한다.
  return false;
};
```

**2) `risk-monitor` Edge Function** — 구독자를 좌표 격자로 묶어 위험도를 계산한다.
같은 격자의 구독자는 한 번만 계산한다.

**3) pg_cron 등록**

```sql
select cron.schedule(
  'risk-monitor-5min',
  '*/5 * * * *',
  $$ select net.http_post(
       url := 'https://<project>.supabase.co/functions/v1/risk-monitor',
       headers := '{"Authorization": "Bearer <service_role_key>"}'::jsonb
     ) $$
);
```

#### 적용 기준

- **주기**: 5분. 기상청 초단기실황이 10분 주기라 그보다 짧게 돌 이유가 없고,
  5분이면 등급 상승을 놓치지 않는다.
- **발송 조건**: 등급 **상승** 시에만. 하락 시에는 발송하지 않고 `last_notified_level`만 갱신한다.
  "위험이 해제됐다"는 알림은 가치가 낮고 알림 피로를 키운다.
- **재발송 쿨다운**: 같은 등급 유지 시 1시간. 폭우가 3시간 이어질 때 5분마다 알림이 가면
  사용자가 알림을 끈다. 끄면 정작 필요할 때 못 받는다.
- **격자 크기**: 좌표를 소수점 2자리(약 1.1km)로 반올림해 묶는다. 위험도 계산 비용을 줄인다.
- **발송 상한**: 1회 실행당 최대 1,000건. 초과분은 다음 주기로 넘긴다. Edge Function 타임아웃 방지.

#### 예외 처리

| 상황                                    | 처리                                             |
| --------------------------------------- | ------------------------------------------------ |
| 위험도 `UNKNOWN` (데이터 2개 이상 실패) | 발송하지 않는다. 근거 없는 알림은 신뢰를 깎는다  |
| 상류 API 전체 실패                      | 해당 주기 건너뛰고 로그만 남긴다                 |
| 구독 만료(410)                          | 해당 구독 삭제                                   |
| 발송 실패 (5xx)                         | `failure_count` 증가. 5회 초과 시 비활성화       |
| cron 중복 실행                          | `last_notified_at` 기준 쿨다운이 자연스럽게 방어 |

#### 검증 방법

1. **단위 테스트** — `shouldNotify` (가장 중요)
   - 첫 도달 → `true`
   - WATCH → WARNING 상승 → `true`
   - WARNING → WATCH 하락 → `false`
   - 같은 등급 59분 경과 → `false`
   - 같은 등급 61분 경과 → `true`
   - 임계값 미만 → `false`
   - `UNKNOWN` → `false`
2. **통합** — 테스트 구독을 넣고 `risk-monitor`를 수동 호출해 알림 수신 확인
3. **부하** — 구독 1,000건으로 실행 시간이 Edge Function 타임아웃 이내인지
4. `cron.job` 테이블에서 스케줄 등록 확인

---

## Phase 4. 긴급 모드·음성 안내

> 상세서 4.3에 명세돼 있으나 구현되지 않은 부분. 설계 문서와 구현의 간극이 가장 큰 지점이라
> 문서를 함께 제출하면 대조당하기 쉽다.

### 4-A. 심각 단계 긴급 모드

#### 작업 내용

위험도가 `CRITICAL`일 때 화면을 단일 행동 중심으로 전환한다.

`EmergencyBar` 컴포넌트는 이미 만들어져 있으나 **어디서도 렌더되지 않는 데드 코드**다.

#### 수정 대상

- `src/routes/index.tsx` — 긴급 레이아웃 분기, `EmergencyBar` 렌더
- `src/components/layout/EmergencyBar.tsx` — `onAction` 연결
- `src/components/risk/ActionCard.tsx` — 긴급 상태 스타일 (이미 `isCritical` 분기 존재, 확장)
- `src/routes/routes.tsx` — 대안 경로 접기

#### 구현 방법

```tsx
// src/routes/index.tsx
const isCritical = riskLevel === "CRITICAL";

// 하단 고정 바
{
  hasSelectedLocation && isCritical && (
    <EmergencyBar
      onAction={() => {
        navigate({ to: "/routes", search: { mode: "safest", auto: true } });
      }}
    />
  );
}
```

`/routes`는 `search.auto === true`일 때 추천 경로만 펼치고 대안·제외 경로는 접은 상태로 렌더한다.

#### 적용 기준

- **발동 조건**: `riskLevel === "CRITICAL"` 단독. 점수(86점 이상)가 아니라 등급으로 판정한다.
  등급 산출은 `scoreToLevel`이 이미 담당한다.
- **화면에 노출할 주요 행동 수: 1개.** 상세서 4.3의 "선택 부담을 줄인다" 원칙.
  긴급 상태에서 대안 경로 3개를 나란히 보여주면 판단이 늦어진다.
- **되돌리기**: 등급이 내려가면 자동으로 일반 레이아웃으로 복귀한다. 사용자가 수동으로
  긴급 모드를 끌 수는 없게 한다 (실수로 끄면 위험).
- **`UNKNOWN` 등급**: 긴급 모드를 발동하지 않는다. 데이터가 없는 것이지 위험한 것이 아니다.

#### 예외 처리

| 상황                             | 처리                                                                     |
| -------------------------------- | ------------------------------------------------------------------------ |
| 경로 계산 실패                   | 긴급 바는 유지하되 "경로를 계산할 수 없습니다 · 대피소 목록 보기"로 대체 |
| 대피소 후보 없음                 | 긴급 바를 "119 신고" 동작으로 전환                                       |
| 위험도가 CRITICAL↔WARNING을 오감 | 등급 변경에 30초 디바운스를 걸어 화면이 깜빡이지 않게 한다               |

#### 검증 방법

1. 시나리오 프리셋으로 `CRITICAL` 설정 → 긴급 바 노출, 단일 CTA 확인
2. `WARNING`으로 낮춤 → 일반 레이아웃 복귀
3. `grep -rn "EmergencyBar" src --include="*.tsx"` 결과에 사용처가 있는지
4. 컴포넌트 테스트: `CRITICAL`일 때만 렌더되는지
5. 등급을 빠르게 토글해 화면 깜빡임이 없는지

---

### 4-B. 음성 안내

#### 작업 내용

Web Speech API로 행동 카드와 경로 안내를 낭독한다. 코드에 `speechSynthesis` 흔적이 전혀 없다.

비 오는 밤 운전 중이거나 물이 차오르는 상황에서는 화면을 볼 수 없다.
브라우저 내장이라 **비용 0원, 작업량 반나절**인데 접근성 배점에서 얻는 것이 크다.

#### 수정 대상

- `src/lib/speech/speak.ts` — 신규
- `src/hooks/useSpeech.ts` — 신규
- `src/components/risk/ActionCard.tsx` — 재생 버튼
- `src/store/scenario.ts` 또는 설정 스토어 — 음성 안내 on/off

#### 구현 방법

```ts
// src/lib/speech/speak.ts
export const isSpeechSupported = () => typeof window !== "undefined" && "speechSynthesis" in window;

export const speak = (text: string, options: { lang?: string; rate?: number } = {}) => {
  if (!isSpeechSupported() || !text.trim()) return false;
  window.speechSynthesis.cancel(); // 이전 낭독 중단
  const utterance = new SpeechSynthesisUtterance(text);
  utterance.lang = options.lang ?? "ko-KR";
  utterance.rate = options.rate ?? 0.95;
  window.speechSynthesis.speak(utterance);
  return true;
};

export const stopSpeaking = () => {
  if (isSpeechSupported()) window.speechSynthesis.cancel();
};
```

#### 적용 기준

- **낭독 문구는 화면 텍스트와 동일하게 유지한다.** 별도 문구를 만들면 정보가 갈라지고,
  화면과 음성이 다른 말을 하면 신뢰가 깨진다.
- **자동 낭독**: `CRITICAL` 진입 시 **1회만**. 반복 낭독하지 않는다.
- **수동 재생**: 모든 등급에서 행동 카드의 재생 버튼으로 가능.
- **속도 0.95**: 기본 속도는 재난 상황에서 다소 빠르다. 약간 늦춘다.
- **사용자 토글**: 설정에서 끌 수 있게 한다. 기본값은 **꺼짐**.
  자동 재생되는 소리는 놀라움을 유발할 수 있어 옵트인이 맞다.

#### 예외 처리

| 상황                     | 처리                                              |
| ------------------------ | ------------------------------------------------- |
| `speechSynthesis` 미지원 | 재생 버튼을 렌더하지 않는다. 오류 표시 금지       |
| 한국어 음성 미설치       | 브라우저 기본 음성으로 재생. 실패해도 조용히 무시 |
| 자동재생 정책 차단       | 첫 사용자 상호작용 이후에만 자동 낭독을 시도한다  |
| 화면 이탈                | `useEffect` cleanup에서 `stopSpeaking()` 호출     |
| 중복 재생                | `speak` 진입 시 `cancel()` 선행                   |

#### 검증 방법

1. 행동 카드 재생 버튼 클릭 → 한국어 낭독
2. 재생 중 다른 화면 이동 → 낭독 중단
3. 미지원 브라우저 시뮬레이션(`delete window.speechSynthesis`) → 버튼 미노출, 크래시 없음
4. 단위 테스트: `isSpeechSupported` 분기, `speak`가 미지원 시 `false` 반환

---

## Phase 5. 목데이터 제거

> 같은 질문에 화면마다 다른 답이 나오는 문제. 심사위원이 탭을 번갈아 누르면 바로 보인다.

### 5-A. `/help` 실데이터 전환

#### 작업 내용

`/help`가 목 데이터 `SHELTERS`와 `buildRoutes`를 쓰고 있다. 홈은 실제 훅을 쓴다.
지방에서 시연하면 홈은 현재 위치 대피소를, 도움 탭은 강남 대피소를 안내한다.

연동 문서에 "홈 화면 Gemini 입력은 mock route generator를 사용하지 않는다"고 명시해 놓고
`/help`는 그대로 쓰고 있는 상태다.

#### 수정 대상

- `src/routes/help.tsx` — 4·38·40·57번 줄 일대

#### 구현 방법

`src/routes/index.tsx`의 데이터 조립 방식을 그대로 옮긴다.

```tsx
// 변경 전
const nearest = [...SHELTERS].filter(...)...;
const recommendedRoute = buildRoutes(origin).find(...);

// 변경 후
const { shelters } = useShelters(origin);
const { events: trafficEvents } = useTrafficEvents(origin);
const routeState = useRoutes({ origin, shelters, trafficEvents, enabled: shelters.length > 0 });
const recommendedRoute = routeState.routes.find((r) => r.status === "RECOMMENDED");
```

`dataTimestamp`는 Phase 1-A의 집계 시각을 쓴다.

`allowedProperNouns`에 하드코딩된 `"서울 강남구"`, `"강남역"`, `"역삼동"`, `"탄천"`을
실제 역지오코딩 결과(`useReverseGeocode`)와 대피소명으로 교체한다.
이 배열은 AI 응답 검증에서 허용 고유명사로 쓰이므로, 하드코딩이 남으면
지방 사용자에게 강남 지명이 섞인 답이 나갈 수 있다.

#### 적용 기준

- 홈과 `/help`는 **같은 훅, 같은 파라미터**를 쓴다. 두 화면의 답이 달라선 안 된다.
- 대피소가 아직 로딩 중이면 AI 질문 버튼을 비활성화한다. 빈 데이터로 질문하면
  `shouldCallGemini`가 false가 되어 규칙 기반 답변만 나온다.

#### 예외 처리

| 상황             | 처리                                                             |
| ---------------- | ---------------------------------------------------------------- |
| 대피소 조회 실패 | `shelterApi`의 기존 fallback(`public/data/shelters.json`)이 동작 |
| 경로 계산 실패   | `recommendedRoute` undefined → 대피소 기준으로만 질문 구성       |
| 위치 미허용      | 주소 입력 흐름으로 유도. 목 데이터로 대체하지 않는다             |

#### 검증 방법

1. `grep -n "buildRoutes\|SHELTERS" src/routes/help.tsx` 결과 없음
2. 위치를 지방(예: 부산)으로 설정 후 홈과 `/help`의 대피소명이 일치하는지
3. 기존 테스트 `src/routes/-index.test.tsx` 통과 유지

---

### 5-B. 운영 화면 위험구역 동적 산출

#### 작업 내용

`/ops/risk-zones`와 `/ops/messages`가 강남 3개로 하드코딩된 `RISK_ZONES`를 쓴다.

#### 수정 대상

- `src/lib/ops/riskZoneBuilder.ts` — 신규
- `src/routes/ops/risk-zones.tsx`
- `src/routes/ops/messages.tsx`
- `src/routes/ops/shelters.tsx` — 가짜 운영 데이터 제거 (28·50번 줄)
- `src/lib/ops/aggregateRiskZones.ts` — `CONTROL_ROADS` 하드코딩 제거

#### 구현 방법

관심 지역을 격자로 나누고 각 격자의 위험도를 계산해 상위 N개를 위험구역으로 삼는다.

1. 관심 영역을 500m 격자로 분할
2. 격자별로 SafeMap 침수흔적·하천범람 중첩률 조회 (`wmsFeatureInfo` 재사용)
3. 반경 내 수위 센서·특보·재난문자 반영
4. `calculateRiskScore`로 격자 점수 산출
5. 상위 5개를 `RiskZone` 형태로 변환

`aggregateRiskZones.ts`의 `CONTROL_ROADS`는 `rz-1`~`rz-3` 키에 강남 도로명이 하드코딩돼 있다.
이를 실제 ITS 돌발상황의 `roadName`으로 대체한다.

#### 적용 기준

- **격자 크기 500m**: 더 잘게 나누면 WMS 조회 횟수가 폭증한다. 500m면 생활권 단위 판단에 충분하다.
- **상위 5개**: 상세서 4.4가 "TOP 5"를 명시한다.
- **캐시**: 격자별 WMS 중첩률은 10분 캐시. 침수흔적도는 정적 데이터라 자주 조회할 이유가 없다.
- **관심 영역**: 담당자가 선택한 행정구역. 기본값은 지도 현재 bounds.

#### 예외 처리

| 상황           | 처리                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| WMS 조회 실패  | 해당 격자 중첩률 0으로 처리 (기존 `wmsFeatureInfo` 동작과 동일)                      |
| 격자 전체 실패 | "위험구역을 산출할 수 없습니다 · 데이터 상태 확인" 안내. 목 데이터로 되돌리지 않는다 |
| 위험구역 0개   | "현재 기준 위험구역 없음"을 정상 상태로 표시                                         |
| 조회 시간 초과 | 격자 수를 줄여 재시도. 상위 5개만 필요하므로 전수 조사가 필수는 아니다               |

#### 검증 방법

1. `grep -rn "RISK_ZONES" src/routes/ src/lib/ | grep -v test` 결과 없음
2. 서울·부산 등 다른 지역에서 위험구역이 산출되는지
3. 위험구역 0개 상태가 오류가 아닌 정상 화면으로 렌더되는지
4. 단위 테스트: 격자 분할, 상위 N 선별, 빈 결과 처리

---

### 5-C. 데이터 상태 실측 전환

#### 작업 내용

`/ops/data-health`의 API 상태가 `scenarioPresetId` 프리셋 값에서 나온다. 실제 호출 결과가 아니다.
운영 신뢰성을 보여주려고 만든 화면이 시뮬레이션이면 역효과가 난다.

#### 수정 대상

- `src/store/apiHealth.ts` — 신규 (실측 상태 수집 스토어)
- `src/routes/ops/data-health.tsx` — 프리셋 의존 제거
- `src/hooks/useApiStatus.ts` — 실측 상태 연결
- 각 Edge Function — 응답에 `durationMs` 추가

#### 구현 방법

**1) 각 훅이 이미 `ApiResult`를 반환한다.** 이를 전역 스토어에 모은다.

```ts
// src/store/apiHealth.ts
interface ApiHealthState {
  sources: Record<
    string,
    { status: ApiStatus; timestamp: string; durationMs?: number; error?: string }
  >;
  report: (name: string, result: ApiResult<unknown>, durationMs?: number) => void;
}
```

**2) 훅에서 결과를 보고한다.** `useWeather`, `useWeatherWarnings`, `useDisasterMessages`,
`useTrafficEvents`, `useSensorFeeds`, `useShelters`, `useRoutes` 각각의 `queryFn` 종료 시점.

**3) `data-health.tsx`가 스토어를 읽어 렌더한다.**

**4) 24시간 추이** — `api_health_metrics` 테이블에 자동 적재하고 스파크라인으로 표시한다.
현재는 담당자가 버튼을 눌러야만 기록된다.

#### 적용 기준

- **자동 적재 주기**: 매 호출마다 적재하면 행이 폭증한다. **5분에 1회**로 스로틀한다.
- **응답시간 측정**: 클라이언트 기준(`performance.now()` 차이). Edge Function 내부 시간이 아니라
  사용자가 체감하는 시간이 의미 있다.
- **시나리오 프리셋 유지 여부**: 데모용 프리셋 기능은 남기되, `/ops/data-health`에서는
  실측만 표시한다. 프리셋은 홈 화면 시연용으로만 쓴다.

#### 예외 처리

| 상황                           | 처리                                                     |
| ------------------------------ | -------------------------------------------------------- |
| 아직 호출되지 않은 API         | `"미조회"` 상태로 표시. `OK`로 가정하지 않는다           |
| `api_health_metrics` 적재 실패 | 화면 표시는 계속. 적재 실패를 사용자에게 노출하지 않는다 |
| 스파크라인 데이터 부족         | "수집 중" 표시                                           |

#### 검증 방법

1. `grep -n "scenarioPresetId\|preset" src/routes/ops/data-health.tsx` 결과가 실측과 무관한지
2. 네트워크를 끊고 홈 진입 후 `/ops/data-health`에서 해당 API가 `FAILED`로 뜨는지
3. 5분 대기 후 `api_health_metrics`에 행이 자동 적재됐는지
4. 스로틀이 동작해 행이 과다 생성되지 않는지

---

## Phase 6. 차별화 기능

> 챌린지 가점 영역. Phase 1~5가 "제대로 만든 서비스"를 만든다면 이 Phase가 "왜 이 팀인가"를 만든다.

### 6-A. CCTV 영상 AI 판독

#### 작업 내용

전국 CCTV 스틸 프레임을 Gemini 멀티모달에 넘겨 노면 침수 여부·통행 가능 여부를 판독한다.

**왜 이것이 가장 강력한가**: 현재 데이터에 구멍이 있다.
침수흔적도는 **과거**, 기상 예보는 **미래**인데 _"지금 저 길이 잠겼는가"_ 에 답하는 근거가 없다.
CCTV가 정확히 그 자리를 메운다. 그리고 지금 AI는 설명문 생성에만 쓰여 "AI 기반"이라는
주장의 근거가 약한데, 이 기능이 그것을 해소한다.

#### 수정 대상

- `supabase/functions/cctv-analyze/index.ts` — 신규
- `supabase/functions/_shared/cctvAnalysis.ts` — 판독 결과 스키마·검증 (순수 함수)
- `supabase/migrations/<타임스탬프>_create_cctv_analysis.sql` — 판독 결과 캐시 테이블
- `src/hooks/useCctvAnalysis.ts` — 신규
- `src/components/field/FieldCctv.tsx` — 판독 결과 표시
- `src/lib/risk/calculateRiskScore.ts` — 근거 가산

#### 구현 방법

**1) 프레임 캡처** — CCTV는 HLS 스트림이다. Edge Function에서 세그먼트 1개를 받아
키프레임을 JPEG로 추출한다. `hls.js`는 브라우저용이므로 서버에서는 HLS 매니페스트를 파싱해
첫 `.ts` 세그먼트를 받고 프레임을 뽑는다.

> **대안**: 서버 프레임 추출이 무겁다면 브라우저에서 `<video>` + `canvas.toDataURL()`로
> 캡처해 Edge Function에 전송하는 방식도 가능하다. 이 경우 사용자가 CCTV를 열어본
> 시점에만 판독되므로 자동 감시에는 못 쓴다. **1차 구현은 브라우저 캡처로 시작하고,
> 자동 감시가 필요해지면 서버 추출로 옮긴다.**

**2) Gemini 호출** — 기존 `_shared/vertexAi.ts`에 이미지 파트를 추가한다.
구조화 출력(`responseSchema`)은 이미 `gemini-chat`에서 쓰고 있으므로 같은 방식을 따른다.

```ts
responseSchema: {
  type: "object",
  properties: {
    flooded: { type: "boolean" },
    depthGrade: { type: "string", enum: ["NONE", "SHALLOW", "DEEP", "IMPASSABLE"] },
    passable: { type: "boolean" },
    confidence: { type: "number" },
    observation: { type: "string" },
  },
  required: ["flooded", "depthGrade", "passable", "confidence", "observation"],
}
```

**3) 위험도 반영** — `RiskCalculationInput`에 `cctvFloodEvidence?: { depthGrade, confidence }`를 추가하고,
`floodTrace` 항목과 별도로 가산한다.

#### 적용 기준

- **판정 주체를 바꾸지 않는다.** 위험도 등급은 서버 계산이 정하고, CCTV 판독은 **근거로 가산**한다.
  상세서 2.2의 "Gemini 단독 위험 판단 금지" 원칙을 그대로 지킨다. 이것이 심사 방어 논리다.
- **신뢰도 임계값 0.7.** 미만이면 화면에 표시하지 않고 위험도에도 반영하지 않는다.
- **가산 점수**: `IMPASSABLE` 15점, `DEEP` 10점, `SHALLOW` 5점, `NONE` 0점.
  단독으로 등급을 뒤집지 못하는 크기로 잡는다.
- **호출 범위**: 전국 CCTV를 전부 판독하지 않는다. 다음 조건에서만 호출한다.
  - 위험도 `WARNING` 이상인 지역
  - 사용자 위치 또는 경로 반경 2km 이내
  - 담당자가 명시적으로 요청한 카메라
- **캐시**: 판독 결과 10분. 같은 카메라를 반복 호출하지 않는다.
- **표시**: 판독 원본 프레임과 신뢰도를 함께 보여준다. 사람이 검증할 수 있어야 한다.
  `"AI 판독 · 사람 확인 필요"` 라벨을 붙인다.

#### 예외 처리

| 상황                      | 처리                                                                   |
| ------------------------- | ---------------------------------------------------------------------- |
| HLS 토큰 만료(401)        | CCTV 캐시 수명이 2분이므로 재조회. 기존 커밋 `6ba55c5` 참조            |
| 프레임 추출 실패          | 판독 생략. 위험도에 영향 없음                                          |
| Gemini 타임아웃           | 25초 상한(`GEMINI_CHAT_TIMEOUT_MS`와 동일). 초과 시 판독 없음으로 진행 |
| 신뢰도 < 0.7              | 결과 폐기. 화면·점수 모두 미반영                                       |
| 야간·악천후로 영상 불명확 | 모델이 낮은 신뢰도를 반환하므로 위 규칙으로 자동 처리                  |
| 비용 초과                 | 호출 범위 제한 + 캐시로 방어. 일일 호출 상한을 환경변수로 둔다         |

#### 검증 방법

1. **단위 테스트** — 판독 결과 스키마 검증, 신뢰도 임계값 필터, 점수 매핑
2. **실데이터** — 맑은 날 CCTV로 `flooded: false`가 나오는지
3. **경계** — 신뢰도 0.69/0.70/0.71 처리 확인
4. **비용** — 100회 호출 시 예상 비용 산출, 일일 상한 동작 확인
5. **위험도 영향** — 판독 결과만으로 등급이 뒤집히지 않는지 (설계 원칙 위반 여부)

---

### 6-B. 지하차도 위험 레이어

#### 작업 내용

지하차도 데이터를 적재하고 경로 통과 여부를 판정한다.
`hasUnderpass`가 `false`로 하드코딩된 근본 원인이 데이터 부재다.

지하차도 침수는 제품 정의 첫 문장에 등장하는 핵심 시나리오이고,
차량 경로에서 가장 치명적인 실패 지점이다.

#### 선행 작업 (착수 전 필수)

**데이터 확보 가능 여부를 먼저 확인한다.** 확보 경로가 불확실하므로 다음 순서로 조사한다.

1. 공공데이터포털에서 전국 단위 지하차도 좌표 데이터 검색
2. 없으면 국가교통정보센터(ITS) 시설물 데이터 확인
3. 없으면 지자체별 개방 데이터 (서울열린데이터광장 등)
4. 모두 없으면 **시연 지역 한정 수동 적재**로 축소한다

4번으로 축소하더라도 `hasUnderpass` 인자는 살아나므로 가치가 있다.
전국 커버리지는 포기하되 기능은 증명할 수 있다.

#### 수정 대상

- `supabase/migrations/<타임스탬프>_create_underpasses.sql` — 신규
- `scripts/sync-underpasses.js` — 신규 (`sync-warning-zones.js` 패턴 재사용)
- `src/lib/risk/underpassRisk.ts` — 신규
- `src/lib/risk/routeRanking.ts` — 통과 판정 추가
- `src/hooks/useRiskAssessment.ts` — `hasUnderpass` 배선

#### 구현 방법

경로 polyline과 지하차도 좌표의 최단거리를 계산한다.
`routeRanking.ts`에 이미 `distanceToRouteMeters`가 있으므로 그대로 재사용한다.

```ts
export const routePassesUnderpass = (
  route: RouteResult,
  underpasses: Underpass[],
  radiusMeters = UNDERPASS_MATCH_RADIUS_M,
) =>
  underpasses.some((u) => distanceToRouteMeters({ position: u.position }, route) <= radiusMeters);
```

#### 적용 기준

- **매칭 반경 50m.** ITS 돌발상황(120m)보다 좁게 잡는다. 지하차도는 점이 아니라 구간이지만
  좌표 1점으로 저장되므로, 너무 넓게 잡으면 옆 도로가 걸린다.
- **강우 임계값에 따른 처리 구분**:

  | 조건                                  | 경로 처리         |
  | ------------------------------------- | ----------------- |
  | 시간당 강우 30mm 이상 또는 호우경보   | `REJECTED` (제외) |
  | 시간당 강우 15mm 이상 또는 호우주의보 | 감점 30           |
  | 그 외                                 | 감점 10           |

  임계치를 넘으면 감점이 아니라 **제외**로 처리한다. 지하차도 침수는 되돌릴 수 없는 사고다.

- **도보 경로**: 지하차도는 차량 경로 문제다. 도보 경로에는 적용하지 않는다.
- **위험도 인자**: 현재 위치 반경 500m 내 지하차도 존재 시 `hasUnderpass: true` (5점).

#### 예외 처리

| 상황                      | 처리                                                         |
| ------------------------- | ------------------------------------------------------------ |
| 데이터 미확보 지역        | `hasUnderpass: false`. 커버리지를 화면에 명시한다            |
| 좌표 없는 시설            | 적재 시 제외                                                 |
| 모든 경로가 지하차도 통과 | 전부 제외하지 않고 최소 위험 경로 1개는 남기되 경고를 붙인다 |

> **예외 처리 마지막 항목이 중요하다.** 모든 후보를 `REJECTED`로 만들면 사용자에게
> 아무 경로도 못 준다. 재난 상황에서 "갈 길이 없다"는 최악의 응답이다.

#### 검증 방법

1. 단위 테스트: 반경 내/밖 판정, 강우 임계값별 처리 분기
2. 지하차도를 지나는 경로가 강우 30mm 조건에서 `REJECTED`인지
3. 모든 경로가 제외 대상일 때 최소 1개가 남는지
4. 도보 경로에는 적용되지 않는지
5. `grep -n "hasUnderpass: false" src/hooks/useRiskAssessment.ts` 결과 없음

---

## Phase 7. 포용성·마무리

### 7-A. 이동약자 모드

#### 작업 내용

고령자·이동약자를 위한 큰 글씨, 고대비, 경사·계단 회피 경로, 낮춘 보행속도 기준.

침수 재난에서 인명 피해가 집중되는 집단인데, 현재 서비스는 모든 사용자가 같은 속도로
걷는다고 가정한다. "사회문제 해결" 챌린지에서 포용성은 배점에 직접 걸리기 쉽다.

#### 수정 대상

- `src/store/accessibility.ts` — 신규
- `src/styles.css` — 큰 글씨·고대비 토큰
- `src/lib/api/tmapPedestrian.ts` — 보행자 옵션
- `src/lib/risk/routeRanking.ts` — 보행속도 기준 변경
- 설정 진입점 (`/help` 또는 헤더)

#### 구현 방법

CSS 변수로 스케일을 정의하고 루트에 `data-a11y` 속성을 붙여 전환한다.

```css
:root {
  --font-scale: 1;
}
:root[data-a11y="large"] {
  --font-scale: 1.25;
}
:root[data-a11y="large-contrast"] {
  --font-scale: 1.25;
  --text: #000;
  --border-soft: #666;
}
```

TMAP 보행자 경로 API의 옵션으로 계단 회피를 요청한다.
(옵션 코드는 TMAP 가이드 문서에서 확인해 적용한다.)

#### 적용 기준

- **글씨 배율 1.25배.** 1.5배는 레이아웃이 깨진다. 상세서가 고정한 하단 탭 높이 64px,
  아이콘 22px 규격을 유지하면서 본문만 키운다.
- **보행속도**: 기본 67m/분 → 이동약자 45m/분. 도착 예상시각을 다시 계산한다.
  이 값이 반영되지 않으면 "10분 거리"라고 안내하고 실제로 15분이 걸린다.
- **대비**: WCAG AA(4.5:1) 이상.
- **설정 유지**: `localStorage`에 저장해 재방문 시 유지한다.

#### 예외 처리

| 상황                           | 처리                                                  |
| ------------------------------ | ----------------------------------------------------- |
| TMAP이 계단 회피 옵션 미지원   | 기본 경로를 쓰되 "계단 정보 없음" 표시                |
| 큰 글씨에서 레이아웃 깨짐      | 해당 컴포넌트에 `min-width: 0` + `overflow-wrap` 적용 |
| 고대비에서 지도 마커 식별 곤란 | 마커 크기·테두리를 함께 키운다                        |

#### 검증 방법

1. 이동약자 모드 on/off 전환 시 레이아웃이 깨지지 않는지 (360px 폭 기준)
2. 도착 예상시각이 실제로 늘어나는지
3. `@axe-core/react`로 접근성 위반 0건
4. 대비비 측정 도구로 AA 통과 확인

---

### 7-B. 잔여 정리

#### 작업 내용

- Phase 1-C에서 미뤘던 기존 타입 오류 해소
  - `src/hooks/useTrafficEvents.ts` 30·35·36·41·44행 — `response`가 `unknown`
  - `src/routes/-index.test.tsx` 72·76·84·199행
- CI에서 `continue-on-error` 제거
- `docs/` 갱신 — 각 Phase에서 바뀐 연동 내용 반영
- 시연 리허설 — 지방 좌표 기준으로 전 화면 점검

#### 검증 방법

1. `npx tsc --noEmit` 오류 0건
2. `pnpm run lint` 오류 0건 (경고는 허용)
3. `npx vitest run` 전체 통과, 테스트 수가 212건보다 많은지
4. 서울이 아닌 좌표(예: 부산 해운대)로 전 화면 순회 — 강남 지명이 나오는 곳이 없는지

---

## 부록 A. 환경변수

Phase 진행 중 추가되는 변수. 모두 `.env`(gitignore됨)와 `.env.example`(값 없이)에 등록하고,
서버 변수는 `scripts/set-supabase-secrets.ps1`의 목록에도 추가한다.

| 변수                        | Phase | 구분       | 설명                                             |
| --------------------------- | ----- | ---------- | ------------------------------------------------ |
| `KMA_WARNING_SERVICE_KEY`   | 0     | 서버       | 기상특보. 미설정 시 `KMA_SERVICE_KEY`로 fallback |
| `VITE_VAPID_PUBLIC_KEY`     | 3     | 클라이언트 | 웹푸시 공개키                                    |
| `VAPID_PRIVATE_KEY`         | 3     | 서버       | 웹푸시 개인키. 절대 클라이언트 노출 금지         |
| `VAPID_SUBJECT`             | 3     | 서버       | `mailto:` 형식 연락처                            |
| `CCTV_ANALYSIS_DAILY_LIMIT` | 6     | 서버       | Gemini 판독 일일 호출 상한                       |

> **주의**: `FCM_SERVER_KEY`는 Phase 3 완료 후 제거한다. 레거시 FCM은 종료됐다.

## 부록 B. 배포 절차

```bash
# 1. 검증
pnpm run lint
npx tsc --noEmit -p tsconfig.json
npx vitest run

# 2. 시크릿 등록 (변경 시에만)
pnpm run supabase:secrets

# 3. 마이그레이션 + 함수 배포
pnpm run supabase:deploy
```

신규 Edge Function은 반드시 아래 두 곳에 등록돼 있어야 배포된다.

- `supabase/config.toml` → `[functions.<이름>]`
- `scripts/deploy-supabase-all.ps1` → 함수 이름 배열

## 부록 C. 리스크와 대응

| 리스크                   | 영향                               | 대응                                                            |
| ------------------------ | ---------------------------------- | --------------------------------------------------------------- |
| 기상특보 활용신청 미승인 | Phase 0 결과가 fallback으로만 동작 | 자동승인이므로 즉시 신청. 반영에 최대 1시간                     |
| 지하차도 데이터 미확보   | Phase 6-B 축소                     | 시연 지역 한정 수동 적재로 대체 (기능은 증명 가능)              |
| CCTV 판독 비용 초과      | Phase 6-A 중단                     | 호출 범위 제한 + 일일 상한. 담당자 요청 시에만 호출로 축소 가능 |
| iOS 웹푸시 제약          | Phase 3 효과 반감                  | 홈 화면 추가 안내 문구. Android·데스크톱은 정상 동작            |
| 일정 부족                | 전체 미완                          | Phase 1 → 2 → 3 순으로 사수. Phase 6·7은 선택                   |

## 부록 D. 우선순위 요약

일정이 부족할 때 버릴 순서 (뒤에서부터 버린다).

1. **절대 사수** — Phase 1 (데이터 신뢰성). 이게 없으면 나머지가 무의미하다.
2. **사수** — Phase 2 + 3 (PWA·알림). 재난 서비스의 성립 조건.
3. **강력 권장** — Phase 4 (긴급 모드·음성). 상세서 준수 + 저비용 고효과.
4. **권장** — Phase 5 (목데이터 제거). 시연 중 노출 위험.
5. **차별화** — Phase 6-A (CCTV AI). 시간이 되면 이것부터.
6. **선택** — Phase 6-B, Phase 7.
